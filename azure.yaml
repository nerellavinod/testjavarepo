name: testjavarepo
services:
  app:
    project: ./src/AppHost
    language: dotnet
    host: containerapp

infra:
  provider: "bicep"
  path: "./infra"
  parameters:
    acrName: ${acrName}
    environmentName: ${environmentName}
    location: ${location}
    principalId: ${principalId}
