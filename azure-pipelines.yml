trigger:  
- UiAngulatTest

variables:
  azureSubscription: 'TempAugserviceconnection1'   # use the new connection
  AZURE_SUBSCRIPTION_ID: 'c3d246d3-988a-45ae-ba70-f7faac1e2d0a'
  ARM_TENANT_ID: 'add1c500-a6d7-4dbd-b890-7f8cb67d7861'

  # Resource Names
  resourceGroupName: 'TempAugRG'
  location: 'uksouth'
  webAppName: 'hmnao-ui-app'
  TF_STORAGE_ACCOUNT: 'tempaugstorage'
  TF_STORAGE_CONTAINER: 'tfstate'

stages:

# ============================================================
# 1️⃣ TERRAFORM INFRA DEPLOYMENT
# ============================================================
- stage: IaC
  displayName: "Provision Infra using Terraform"
  jobs:
  - job: TerraformJob
    pool:
      vmImage: 'ubuntu-latest'

    steps:
    - checkout: self

    # Install Terraform manually
    - script: |
        sudo apt-get update
        sudo apt-get install -y wget unzip
        wget https://releases.hashicorp.com/terraform/1.9.8/terraform_1.9.8_linux_amd64.zip
        unzip terraform_1.9.8_linux_amd64.zip
        sudo mv terraform /usr/local/bin/
        terraform -version
      displayName: "Install Terraform"

    # 🔐 Login to Azure using the service connection (SPN / OIDC)
    # and expose credentials as ARM_* env vars for Terraform
    - task: AzureCLI@2
      displayName: "Terraform Init/Plan/Apply"
      inputs:
        azureSubscription: $(azureSubscription)
        scriptType: bash
        scriptLocation: inlineScript
        addSpnToEnvironment: true
        inlineScript: |
          cd Deployment/src

          echo "=== Terraform init ==="
          terraform init \
            -backend-config="resource_group_name=$(resourceGroupName)" \
            -backend-config="storage_account_name=$(TF_STORAGE_ACCOUNT)" \
            -backend-config="container_name=$(TF_STORAGE_CONTAINER)" \
            -backend-config="key=hmnao.tfstate"

          echo "=== Terraform plan ==="
          terraform plan \
            -var "resource_group_name=$(resourceGroupName)" \
            -var "location=$(location)" \
            -var "app_service_name=$(webAppName)"

          echo "=== Terraform apply ==="
          terraform apply -auto-approve \
            -var "resource_group_name=$(resourceGroupName)" \
            -var "location=$(location)" \
            -var "app_service_name=$(webAppName)"

# ============================================================
# 2️⃣ DEPLOY ANGULAR APP TO WEB APP
# ============================================================
- stage: Deploy
  displayName: "Deploy Angular SSR App"
  dependsOn: IaC
  jobs:
  - template: Deployment/templates/continuous-deployment.yml
    parameters:
      azureSubscription: ${{ variables.azureSubscription }}
      webAppName: ${{ variables.webAppName }}
