trigger:
- UiAngulatTest

variables:
  azureSubscription: 'TempAugserviceconnection1'
  ARM_CLIENT_ID: '26533ce6-9d6c-413b-b4bd-7847aab1f2a2'
  ARM_TENANT_ID: 'add1c500-a6d7-4dbd-b890-7f8cb6f7d861'
  ARM_SUBSCRIPTION_ID: 'c3d246d3-988a-45ae-ba70-f7faac1e2d0a'

  # Resource details
  resourceGroupName: 'TempAugRG'
  location: 'uksouth'
  webAppName: 'hmnao-ui-app'

stages:

# ============================================================
# 1️⃣ TERRAFORM INFRA DEPLOYMENT (OIDC AUTH)
# ============================================================
- stage: IaC
  displayName: "Provision Infra using Terraform"
  jobs:
  - job: TerraformJob
    pool:
      vmImage: 'ubuntu-latest'

    steps:
    - checkout: self

    # Install Terraform manually
    - script: |
        sudo apt-get update
        sudo apt-get install -y wget unzip
        wget https://releases.hashicorp.com/terraform/1.9.8/terraform_1.9.8_linux_amd64.zip
        unzip terraform_1.9.8_linux_amd64.zip
        sudo mv terraform /usr/local/bin/
        terraform -version
      displayName: "Install Terraform"

    - task: AzureCLI@2
      displayName: "Terraform Init/Plan/Apply (OIDC Auth)"
      inputs:
        azureSubscription: $(azureSubscription)
        scriptType: bash
        scriptLocation: inlineScript
        addSpnToEnvironment: true
        useGlobalConfig: false
        inlineScript: |

          set -e

          WORKDIR="$(Build.SourcesDirectory)/Deployment/src"

          echo "=== Export OIDC Credentials for Terraform ==="
          export ARM_CLIENT_ID=$AZURE_CLIENT_ID
          export ARM_TENANT_ID=$AZURE_TENANT_ID
          export ARM_SUBSCRIPTION_ID=$AZURE_SUBSCRIPTION_ID
          export ARM_USE_OIDC=true

          echo "ARM_CLIENT_ID=$ARM_CLIENT_ID"
          echo "ARM_TENANT_ID=$ARM_TENANT_ID"
          echo "ARM_SUBSCRIPTION_ID=$ARM_SUBSCRIPTION_ID"

          echo "=== CLEAN TERRAFORM CACHE ==="
          rm -rf $WORKDIR/.terraform
          rm -f $WORKDIR/.terraform.lock.hcl

          echo "=== TERRAFORM INIT ==="
          terraform -chdir=$WORKDIR init -reconfigure

          echo "=== TERRAFORM PLAN ==="
          terraform -chdir=$WORKDIR plan \
            -var "resource_group_name=$(resourceGroupName)" \
            -var "location=$(location)" \
            -var "app_service_name=$(webAppName)"

          echo "=== TERRAFORM APPLY ==="
          terraform -chdir=$WORKDIR apply -auto-approve \
            -var "resource_group_name=$(resourceGroupName)" \
            -var "location=$(location)" \
            -var "app_service_name=$(webAppName)"

# ============================================================
# 2️⃣ DEPLOY ANGULAR APP
# ============================================================
- stage: Deploy
  displayName: "Deploy UI App"
  dependsOn: IaC
  jobs:
  - template: Deployment/templates/continuous-deployment.yml
    parameters:
      azureSubscription: ${{ variables.azureSubscription }}
      webAppName: ${{ variables.webAppName }}
